export default function managePosts(
    state = {
        posts: []
    }, 
    action) {
        let posts;
        let reqObj;
        const jwt = localStorage.getItem("jwt")
    switch (action.type) {
        reqObj
        case "GET_POSTS":
        
         posts = (``)

        default:
            return state;
    }
}